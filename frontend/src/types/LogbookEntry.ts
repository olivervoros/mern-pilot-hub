export interface LogbookEntry {
  id: number;
  title: string;
  departureIcao: string;
  arrivalIcao: string;
  aircraftType: string;
  departureTime: string;
  arrivalTime: string;
  additionalInfo: string;
}
